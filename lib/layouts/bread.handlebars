<div class="{{classPrefix}}">
  {{#each breads}}
    {{#if route}}
      <a href="/#/{{route}}">{{__ title}}</a>
    {{else}}
      <span>{{__ title}}</span>
    {{/if}}
    {{#unless @last}}>{{/unless}}
  {{/each}}
</div>
